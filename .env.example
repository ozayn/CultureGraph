# Optional local convenience — NOT used by Railway.
#
# Per-service templates (recommended for local setup):
#   cp backend/.env.example backend/.env
#   cp frontend/.env.example frontend/.env.local
#
# Uncomment below only if you prefer a single root file for scripts/dev.sh
# or scripts/start.sh. Service-specific files take precedence when loaded
# after this file.

# DATABASE_URL=postgresql://culturegraph:culturegraph@localhost:5432/culturegraph
# NEXT_PUBLIC_API_URL=http://localhost:8000
# CORS_ORIGINS=http://localhost:3000
