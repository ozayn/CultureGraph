# Optional dependencies for building/running the visual artwork index.
# Install with: pip install -r requirements-visual.txt
-r requirements.txt
torch>=2.4.0
torchvision>=0.19.0
open-clip-torch>=2.26.0
